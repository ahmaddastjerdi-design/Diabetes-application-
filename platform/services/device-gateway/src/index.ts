/**
 * @diabetes-quest/device-gateway — public entry. Re-exports the framework-free
 * mapping + offline-queue core. The HTTP/vendor-cloud server lives in `server.ts`.
 */
export * from "./core/index.js";
