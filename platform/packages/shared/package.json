{
  "name": "@diabetes-quest/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain + FHIR contracts for the Diabetes Quest platform. The single source of truth all apps and services import.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./domain": "./dist/domain/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  },
  "license": "SEE LICENSE IN ../../../LICENSE"
}
