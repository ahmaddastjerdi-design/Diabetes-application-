{
  "name": "@diabetes-quest/clinical",
  "version": "0.0.0",
  "private": true,
  "description": "Clinical decision-support logic (AGP / time-in-range / variability / risk) for the clinician panel. Pure, no runtime deps.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  },
  "license": "SEE LICENSE IN ../../../LICENSE"
}
