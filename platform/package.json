{
  "name": "@diabetes-quest/platform",
  "version": "0.0.0",
  "private": true,
  "description": "Diabetes Quest clinical-grade platform monorepo (see docs/specification).",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "SEE LICENSE IN ../LICENSE"
}
